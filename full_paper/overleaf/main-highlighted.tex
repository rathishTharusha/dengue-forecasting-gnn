% Overleaf: set this as the main document (Menu -> Main document) to build the
% contribution-highlighted version. It defines one flag and defers entirely to
% main.tex -- there is only ever one copy of the text.
\def\HIGHLIGHTON{1}
\input{main.tex}
